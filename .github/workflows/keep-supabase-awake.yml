name: Keep Supabase Awake

on:
  schedule:
    - cron: "17 2 1,6,11,16,21,26 * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping database health endpoint
        run: curl --fail --retry 2 https://YOUR-SITE.vercel.app/api/health
